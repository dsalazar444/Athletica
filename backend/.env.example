# ============================================================
#  Athletica Backend — Plantilla de Variables de Entorno
#  Copia este archivo como .env y rellena con tus valores reales
# ============================================================

# Django
SECRET_KEY=cambia-esto-por-una-clave-secreta-larga-y-aleatoria
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos PostgreSQL
DB_NAME=users_db
DB_USER=users_django
DB_PASSWORD=tu_contraseña_aqui
DB_HOST=localhost
DB_PORT=5433
# AI
GEMINI_API_KEY=tu_api_key_de_gemini_aqui

# Configuración de Email REAL (Gmail)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tucorreo@gmail.com
EMAIL_HOST_PASSWORD= clave
DEFAULT_FROM_EMAIL=Athletica <noreply@athletica.com>