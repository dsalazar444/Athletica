import 'package:flutter/material.dart';

// Class with predefined radius for inputs, buttons, etc.
class AppRadius {
  AppRadius._();

  static const BorderRadius card = BorderRadius.all(Radius.circular(24));
  static const BorderRadius cardLarge = BorderRadius.all(Radius.circular(32));
  static const BorderRadius input = BorderRadius.all(Radius.circular(18));
  static const BorderRadius button = BorderRadius.all(Radius.circular(20));
  static const BorderRadius chip = BorderRadius.all(Radius.circular(100));

}

